# Dependencies
node_modules/

# Auth tokens (contain secrets — never commit)
auth.json
reseller-auth.json

# Test artifacts
test-results/
playwright-report/

# OS files
.DS_Store

# MCP config (contains secret keys)
.mcp.json

# Playwright MCP session data
.playwright-mcp/

# Screenshots (local only)
screenshots/
reseller2-auth.json
